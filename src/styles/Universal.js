import { makeStyles } from "@mui/styles";
const useStylesUniversal = makeStyles((theme) => ({
  universalButton: {
    backgroundColor: "#2285e9 !important",
    width: "100%",
  },
}));

export default useStylesUniversal;
